# CI workflow smoke test

This file exists only to create a harmless diff so the repository can open a pull request and validate the current GitHub Actions workflow.

It does not affect application behavior.
